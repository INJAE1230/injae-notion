# Claude Code 지침

## 모델 전환 권유

작업 시작 전 복잡도를 판단해서 모델 전환을 권유한다.

- **단순 작업** (필드 추가, 패턴 반복 수정, 단일 파일 수정, 오타 수정 등) → `/model claude-sonnet-4-6` 전환 제안
- **복잡한 작업** (설계·아키텍처 판단, 여러 파일 리팩토링, 긴 맥락 추적, 트레이드오프 분석 등) → `/model claude-opus-4-6` 전환 권유
- **현재 모델이 적합하면** 별도 언급 없이 바로 진행
