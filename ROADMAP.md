# 로드맵

2026-07-16 전체 프로젝트 감사(11개 카테고리 점검) 결과로 도출된 로드맵. 단기/중기/장기
18개 항목은 전부 완료되어 아래쪽에 이력으로 남기고, 이후 작업과 미착수 항목을 위에
이어 적는다. 각 완료 항목의 커밋 해시로 상세 변경 내용을 확인할 수 있다.

**미착수 항목은 "UI/UX 백로그"와 "기능 후보" 두 섹션에 있다.**

## 감사 이후 추가 작업 (2026-07-18)

방향성 진단(DECISIONS.md) 이후 "AI pull → push 전환" 전략에 따라 진행한 작업.

- [x] 대시보드 AI 아침 브리핑 (`d0df0b9`) — 기한 초과·오늘 업무·진행 중·주간 완료를
      종합해 2~4문장 브리핑. localStorage 날짜별 캐시로 하루 1회만 API 호출
- [x] 의사결정 기록 문서 신설 (`2969af4`) — DECISIONS.md에 앱 방향 진단·AI 전략·
      도구 선택 기준·요아정/BN 조사 기록
- [x] 업무 검색 개선 (`9197073`) — 검색 대상에 입력원본·성과/결과 추가, 공백 구분
      다중 키워드 AND 매칭, AI 비서 검색 도구에 content·originalText 반환 +
      키워드 실패 시 의미 검색 폴백
- [x] AI 비서 쓰기 능력·맥락 인식·대화 유지 + 커맨드 팔레트 액션 (`c300c06`) —
      `addWorkLog`/`updateWorkLogFields` 도구 추가(삭제는 의도적 제외), 현재 pathname을
      요청에 실어 지시어 해석, 대화 localStorage 보존, 쓰기 후 `router.refresh()`
- [x] 대시보드 분석 차트 섹션 접기 (`bc82886`) — 차트 5개를 기본 닫힘으로. 방치돼 있던
      `CollapsibleSection`을 재사용하고 열림 상태를 localStorage에 유지

## UI/UX 백로그 (2026-07-18 전체 점검에서 발견)

전체 화면 점검 결과 남은 항목. 위에서부터 우선순위 순이며, 각 항목에 문제·대상 파일·
제안 해법을 적어 다른 PC에서도 바로 이어받을 수 있게 한다.

- [ ] **모바일 "추가" 버튼이 무거운 폼으로 보낸다** — `mobile-bottom-nav.tsx`의 가운데 +
      버튼이 `/logs/new`(필드 10개 이상)로 이동. 모바일에서 가장 잦은 행동은 "방금 한 일
      기록"인데 가장 무거운 화면으로 보내는 셈.
      → 하단 시트로 빠른 메모(`QuickMemoInput`)를 띄우고, 상세 입력이 필요할 때만
      폼으로 가는 링크를 두는 방식 제안. 음성 입력은 OS 키보드 받아쓰기로 충분(코드 불요).
- [ ] **AI 비서 답변이 평문이라 읽기 어렵다** — `ai-assistant.tsx`의 `MessageBubble`이
      텍스트를 그대로 출력해 목록·표를 요청하면 `-`, `**`가 노출된다. 답변에 언급된
      업무로 이동할 수단도 없음.
      → 경량 마크다운 렌더링 + 업무 id를 `/logs/[id]` 링크로 변환.
- [ ] **업무 목록 정렬이 날짜 고정** — `log-table.tsx`에 헤더 클릭 정렬이 없어
      소요시간순·우선순위순으로 볼 방법이 없다.
      → 헤더 클릭 정렬(클라이언트 정렬로 충분, 데이터 규모가 작음).
- [ ] **검색 결과에 매칭 이유가 안 보인다** — `9197073`에서 입력원본·성과까지 검색하게
      되면서, 제목에 없는 단어로 찾으면 왜 나왔는지 알 수 없다.
      → 매칭된 필드의 해당 구간을 발췌해 결과 행에 표시.
- [ ] **대시보드 3개 목록이 모바일에서 세로로 쌓이고 서로 겹친다** — 오늘의 업무·진행 중·
      마감 임박이 같은 업무를 중복 노출(오늘 날짜 + 진행 중이면 두 곳에 등장).
      `InProgressTasks`는 여러 날 걸친 업무 누락을 막으려 의도적으로 추가된 것이라
      단순 병합은 그 의도를 되돌린다.
      → 모바일에서만 세그먼트 전환(탭)으로 묶는 안이 유력. 실사용 빈도 확인 후 결정.
- [ ] **"최근 업무 5건"이 업무 목록 탭과 중복** — 대시보드에서 다시 볼 이유가 있는지
      검토 후, 없으면 제거해 대시보드를 더 짧게.

## 기능 후보 (수요 확인 후 착수)

- [ ] 주간 리뷰 AI 코치 — 리뷰 시작 시 이번 주 회고 + 다음 주 제안 초안 생성.
      DECISIONS.md의 "AI pull → push" 전략에서 아침 브리핑 다음 순위로 지목된 항목
- [ ] 안 쓰는 분석·보고 4탭(통계·성과·보고서·법인) 사이드바 접이식 그룹으로 이동
- [ ] 데이터가 수백 건 쌓인 뒤 재검토: 패턴 분석·업무량 예측·우선순위 자동 추천
      (지금 규모에서는 장식에 그친다고 판단해 보류 — DECISIONS.md 참조)

## 단기 (1주 이내) — 완료 (`d3eafd0`)

- [x] middleware에 `/api/cron/leave-recalc` 인증 예외 추가 — 누락으로 연차 자동 재계산이
      프로덕션에서 전혀 실행되지 않던 버그
- [x] `templateFormSchema` frequency enum을 8종으로 확장 — 3종만 허용해 5종은 저장 시
      400 에러가 나던 버그
- [x] `memo/confirm`에 zod 검증 추가, `payrollFormSchema` 금액 필드에 `min(0)` 추가
- [x] `next` 16.2.9 → 16.2.10 패치 업그레이드
- [x] 죽은 코드 정리 (`header.tsx`, `index.js` 삭제, `template-service.ts` import 정리)
- [x] 메모 파싱 입력 길이 상한(10만자) 추가
- [x] Vercel 프로덕션 환경변수 확인 — 필요한 값 전부 존재 확인, `BULK_DELETE_PASSWORD`는
      이미 없어 조치 불요

## 중기 (1개월 이내) — 완료 (`1688ec1`)

- [x] 캘린더 등 KST 관련 UTC 버그 수정 (`todayStr`, 업무 등록 기본 날짜, CSV 파일명,
      영수증 OCR 날짜 — 총 4곳)
- [x] 사이드바 로그아웃 버튼 추가 (쿠키 파기 경로가 없었음)
- [x] 급여명세서 OCR: Blob에 영구 저장하지 않고 파일을 직접 인식으로 전환
- [x] 문서 갱신 (`PROJECT_CONTEXT.md`의 폐지된 "다음행동" 상태값, `CLAUDE.md` 모델명)
- [x] 트랙/로그 삭제 시 Blob 첨부 정리 + 임의 URL 삭제 방지
- [x] 로그인 rate limit (IP당 10분 5회) + 타이밍 안전 비교
- [x] 월 1회 Notion 전체 백업 크론 — Blob 스토어가 public 전용이라 내용을
      AES-256-GCM으로 암호화해 저장, 데이터 소스별 실패 격리, 최근 12개 보관

## 장기 (그 이후) — 완료 (`733d56c`, `e57840f`)

- [x] payroll/track 대형 컴포넌트 분해 (payroll 1694→1172줄, track 1059→933줄 + 목적별
      파일 분리). hr-dashboard(798줄)는 이득 대비 응집도 저하 우려로 보류
- [x] 대시보드 쿼리 슬림화 — `hideTrackLinked`를 전체 fetch 후 JS 필터에서 Notion
      `relation is_empty` 서버 필터로 전환. 나머지 최적화(기간 필터 등)는 데이터가
      500건을 넘을 때 착수 (현재 49건으로 시기상조)
- [x] 카톡 그룹 파싱 맵-리듀스 재설계 — 청크 단위 MAP(상세 추출) 후 REDUCE(그룹
      배정만 LLM, 실제 병합은 코드)로 전환. 실측으로 유실 디테일 복구 확인
- [x] injae-ops(Supabase) 이관 여부 — **보류로 판단**. 이 앱 HR은 본인 전용, injae-ops는
      타 직원용으로 대상이 겹치지 않아 지금 이관할 이유 없음. 재검토 트리거: 이 앱으로
      본인 외 직원을 관리하게 되거나, injae-ops가 이 앱의 기능(OCR·미사용휴무 등)을
      중복 구현해야 할 때
